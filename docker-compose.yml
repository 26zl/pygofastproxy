version: "3.9"

services:
  proxy:
    build: .
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      - PY_BACKEND_TARGET=${PY_BACKEND_TARGET:-http://host.docker.internal:4000}
      - PY_BACKEND_PORT=${PY_BACKEND_PORT:-8080}
